
Pod::Spec.new do |s|
  s.name         = "HDAlertView"
  s.version      = "1.0.0"
  s.summary      = "A similar system alertView"
  s.homepage     = "https://github.com/Bruce-7/HDAlertView"
  s.license      = "MIT"
  s.author       = { "HeDong" => "57008939@qq.com" }
  s.source       = { :git => "https://github.com/Bruce-7/HDAlertView.git", :tag => s.version }
  s.platform     = :ios, "7.0"
  s.requires_arc = true
  s.source_files = "HDAlertView"

end